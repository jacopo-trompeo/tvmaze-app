const Loading = () => {
	return (
		<div className="h-screen flex justify-center items-center">
			<div className="animate-spin rounded-full h-64 w-64 border-b-4 border-white"></div>
		</div>
	);
};

export default Loading;
